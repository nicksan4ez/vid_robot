VID_ROBOT
=========

Inline-бот Telegram для поиска и отправки коротких видео. Поддерживает два пути:
- Готовые ролики (кэшированные `file_id`) отправляются в чат мгновенно.
- Новые ролики готовятся в личке и затем предлагаются к отправке через inline.

Требования
----------
- Python 3.10+
- Установленный `ffmpeg` в системе (нужен `yt-dlp` для склейки/ремикса аудио/видео)
- Токен бота Telegram

Быстрый старт
-------------
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Создайте `.env` по образцу:
```bash
cp .env.example .env
```

Далее задайте переменные окружения (минимум `BOT_TOKEN`).
Также требуется `YOUTUBE_API_KEY` (YouTube Data API v3).

Запуск
------
```bash
python3 -m src.main
```

Поведение
---------
- В inline-режиме без запроса показывает популярные кэшированные видео.
- При наличии запроса ищет совпадения по прошлым запросам и по названию ролика.
- Кнопка `switch_pm_text` переводит в личку, где бот даёт кнопку для inline-поиска на YouTube.
- Выбор результата в inline запускает подготовку и присылает видео в личку с кнопкой `📤 Отправить в чат…`.

Примечания
----------
- `yt-dlp` запускается как CLI. Если он недоступен в `PATH`, установите пакет через `pip`.
- Значения лимитов и TTL настраиваются в `.env`.
- `INLINE_SHOW_YT_CARDS=true` включает карточки YouTube в inline-выдаче (при выборе отправится нейтральное сообщение).
